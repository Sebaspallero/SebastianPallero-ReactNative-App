import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    message: {
        marginTop: -10,
        marginBottom: 20,
      },
      
      helperText: {
        fontSize: 12,
        fontFamily: 'Poppins-Regular',
        color: 'red',
      },
})

